<Window x:Class="DeskViz.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:DeskViz.App.Views"
        mc:Ignorable="d"
        Title="DeskViz Settings" Height="500" Width="500"
        WindowStartupLocation="CenterOwner" 
        ShowInTaskbar="False" 
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabs for different settings sections -->
        <TabControl Grid.Row="0" Grid.RowSpan="4" Margin="0,0,0,10">
            <TabItem Header="Display">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Preferred Display -->
                    <Label Grid.Row="0" Grid.Column="0" Content="Preferred Display:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Name="DisplayComboBox" VerticalAlignment="Center"
                              DisplayMemberPath="DisplayName"/> 
                              <!-- Changed from DeviceName to DisplayName -->

                    <!-- Widget Orientation -->
                    <Label Grid.Row="1" Grid.Column="0" Content="Widget Layout:" VerticalAlignment="Center" Margin="0,10,10,0"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Name="OrientationComboBox" VerticalAlignment="Center" Margin="0,10,0,0"/>
                </Grid>
            </TabItem>
            
            <TabItem Header="Widgets">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Configure Widgets:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <ListView Grid.Row="1" Name="WidgetsListView" Margin="0,0,0,10">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Enabled" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsWidgetVisible}" VerticalAlignment="Center" 
                                                      Click="WidgetVisibility_Click"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Widget" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Actions" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="↑" Width="24" Margin="2,0" Click="MoveWidgetUp_Click"/>
                                                <Button Content="↓" Width="24" Margin="2,0" Click="MoveWidgetDown_Click"/>
                                                <Button Content="⚙️" Width="24" Margin="2,0" Click="WidgetSettings_Click" ToolTip="Widget Settings"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="ApplyButton" Content="Apply" Width="75" Margin="0,0,10,0" Click="ApplyButton_Click"/>
            <Button Name="OkButton" Content="OK" Width="75" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
