<UserControl x:Class="DeskViz.Widgets.Logo.LogoWidgetSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="400"
             Background="#2D2D30">

    <StackPanel Margin="16">
        <TextBlock Text="Logo/Image Settings"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,16"/>

        <!-- Image Path -->
        <TextBlock Text="Image Path:" Foreground="White" Margin="0,0,0,4"/>
        <DockPanel Margin="0,0,0,12">
            <Button DockPanel.Dock="Right"
                    Content="Browse..."
                    Click="BrowseButton_Click"
                    Padding="12,4"
                    Margin="8,0,0,0"/>
            <TextBox x:Name="ImagePathTextBox"
                     Text="{Binding ImagePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     Padding="4"/>
        </DockPanel>

        <!-- Preview -->
        <Border Background="#1E1E1E"
                CornerRadius="4"
                Height="100"
                Margin="0,0,0,12">
            <Image x:Name="PreviewImage"
                   Stretch="Uniform"
                   Margin="8"/>
        </Border>

        <!-- Width -->
        <TextBlock Text="Width (leave empty for auto):" Foreground="White" Margin="0,0,0,4"/>
        <TextBox x:Name="WidthTextBox"
                 Text="{Binding ImageWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                 Margin="0,0,0,12"
                 Padding="4"/>

        <!-- Height -->
        <TextBlock Text="Height (leave empty for auto):" Foreground="White" Margin="0,0,0,4"/>
        <TextBox x:Name="HeightTextBox"
                 Text="{Binding ImageHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                 Margin="0,0,0,12"
                 Padding="4"/>

        <!-- Stretch Mode -->
        <TextBlock Text="Stretch Mode:" Foreground="White" Margin="0,0,0,4"/>
        <ComboBox x:Name="StretchComboBox"
                  SelectedItem="{Binding Stretch, Mode=TwoWay}"
                  Margin="0,0,0,12">
            <ComboBoxItem Content="None"/>
            <ComboBoxItem Content="Fill"/>
            <ComboBoxItem Content="Uniform"/>
            <ComboBoxItem Content="UniformToFill"/>
        </ComboBox>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Apply" Click="ApplyButton_Click" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Padding="16,6"/>
        </StackPanel>
    </StackPanel>
</UserControl>
